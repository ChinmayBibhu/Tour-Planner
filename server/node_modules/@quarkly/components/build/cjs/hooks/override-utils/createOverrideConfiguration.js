"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _lodash = require("lodash");

/**
 * @param config { Object }
 * */
var mergeDefaultConfig = function mergeDefaultConfig(config) {
  return (0, _lodash.defaults)(config, {
    defaultKey: undefined
  });
};
/**
 * @param customConfig { Object }
 * */


var createOverrideConfiguration = function createOverrideConfiguration(customConfig) {
  var config = customConfig;

  if (!(0, _lodash.isObject)(customConfig)) {
    config = {};
  }

  config = mergeDefaultConfig(config);
  return config;
};

var _default = createOverrideConfiguration;
exports["default"] = _default;